<div class="container">
  <% if @is_company %>
    <h2>Você está se registrando como <strong>empresa</strong></h2>
    <h3>Insira suas informações:</h3>
    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= f.error_notification %>
      <div class="form-inputs">
        <%= f.input :username, 
        required: true, 
        autofocus: true, 
        input_html: { placeholder: "Escolha um username" },
        hint: ("Minimo #{@minimum_password_length} caracteres" if @minimum_password_length) %>
        <%= f.input :email, label: 'Email Empresarial', required: true, autofocus: true, input_html: { placeholder: "seu-email@empresarial.com" } %>
        <%= f.input :password, required: true, hint: ("Minimo #{@minimum_password_length} caracteres" if @minimum_password_length), input_html: { placeholder: "insira uma senha" } %>
        <%= f.input :password_confirmation, required: true, input_html: { placeholder: "confirme sua senha" } %>
      </div>
      <p>Ao registrar-se com um perfil empresarial, você confirma que todas as informações fornecidas são verdadeiras e precisas. Lembre-se de que tanto nós quanto a empresa teremos acesso a esses dados para verificação. Não é permitido fornecer informações falsas ou enganosas. O descumprimento pode levar à suspensão ou exclusão do seu perfil. Agradecemos sua honestidade e cooperação.</p>
      <div>
        <h3>Insira seu cargo na empresa:</h3>
        <%= text_field_tag 'registration_type[role]', nil, required: true, class: 'form-control', placeholder: 'Ex: gerente, representante ...' %>
      </div>
      <div class="form-actions my-4">
        <%= f.button :submit, "Sign up", class: 'btn btn-primary' %>
      </div>
    <% end %>
  <% else %>
    <h2>Sign up</h2>
    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= f.error_notification %>
      <div class="form-inputs">
        <%= f.input :username, 
        required: true, 
        autofocus: true, 
        input_html: { placeholder: "Escolha um username" },
        hint: ("Minimo #{@minimum_password_length} caracteres" if @minimum_password_length)  %>
        <%= f.input :email, required: true, autofocus: true, input_html: { placeholder: "seu@email.com" } %>
        <%= f.input :password, required: true, hint: ("Minimo #{@minimum_password_length} caracteres" if @minimum_password_length), input_html: { placeholder: "insira uma senha" } %>
        <%= f.input :password_confirmation, required: true, input_html: { placeholder: "confirme sua senha" } %>
      </div>
      <div class="form-actions">
        <%= f.button :submit, "Sign up" %>
      </div>
    <% end %>
  <% end %>
  <%= render "devise/shared/links" %>
</div>
